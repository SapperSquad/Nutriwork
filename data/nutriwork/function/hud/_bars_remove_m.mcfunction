# macro: remove all six bars for $(id).
$bossbar remove nutriwork:h$(id)_fruit
$bossbar remove nutriwork:h$(id)_veg
$bossbar remove nutriwork:h$(id)_grain
$bossbar remove nutriwork:h$(id)_meat
$bossbar remove nutriwork:h$(id)_sugar
$bossbar remove nutriwork:h$(id)_water
